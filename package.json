{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
    {
    "require": {
        "php": "^8.0.30",  // Ensure this matches the build environment's PHP version
        // Other dependencies...
    }
}
}
